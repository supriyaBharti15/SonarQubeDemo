# SonarQubeDemo
SonarQube integration with android studio project using local host

##Kotlin
# Mutable and Immutable in Kotlin
```kotlin
# mutable
   var name: String
   name = "supriya"
   Log.d("::TAG::", name)
